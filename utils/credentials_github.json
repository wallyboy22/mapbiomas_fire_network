{
  // Insira o token gerado no GitHub para autenticação.
  // Para gerar um token:
  // 1. Vá para GitHub -> Settings -> Developer Settings -> Personal Access Tokens (classic).
  // 2. Clique em "Generate new token".
  // 3. Selecione as permissões e gere o token.
  // 4. Copie o token e insira-o abaixo no campo "token".
  // 5. Salve este arquivo como 'credentials_github.json' e coloque-o na raiz do seu projeto ou no Google Colab (/content/).

  "github": {
      "token": "sua_chave"  // Substitua "sua_chave" pelo seu token pessoal
  }
}
